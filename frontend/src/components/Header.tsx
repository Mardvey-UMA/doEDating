import React from "react";
import styles from "../../src/styles/headerStyle.module.scss";
const Header: React.FC = () => {
    return (
      <div>className={styles.headerContainer}</div>
  )
});

export default Header;
